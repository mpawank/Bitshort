<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BitShort - URL Shortener</title>
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #ff9a9e, #fad0c4);
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .navbar {
            width: 100%;
            background-color: #ff6363;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            top: 0;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .navbar h1 {
            color: #fff;
            font-size: 1.8rem;
        }

        .navbar ul {
            list-style: none;
            display: flex;
            gap: 1rem;
        }

        .navbar ul li {
            font-size: 1rem;
        }

        .navbar ul li a {
            text-decoration: none;
            color: #fff;
            transition: color 0.3s;
        }

        .navbar ul li a:hover {
            color: #333;
        }

        .container {
            margin-top: 8rem;
            text-align: center;
            background: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 600px;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            animation: fadeIn 1s ease-in-out;
        }

        p {
            margin: 1rem 0;
            font-size: 1.1rem;
        }

        form {
            display: grid;
            gap: 1rem;
        }

        label {
            font-weight: bold;
            text-align: left;
        }

        input[type="text"] {
            padding: 0.8rem;
            font-size: 1rem;
            border: 2px solid #ff6363;
            border-radius: 8px;
            outline: none;
        }

        button {
            padding: 0.8rem 2rem;
            font-size: 1rem;
            color: white;
            background-color: #ff6363;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #333;
        }

        .short-url {
            font-size: 1.2rem;
            background: #ffefba;
            border-radius: 10px;
            padding: 0.8rem;
            display: inline-block;
            margin-top: 1rem;
        }

        /* Animations */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h1>BitShort</h1>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#features">Features</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </div>

    <div class="container">
        <h1>URL Shortener Project</h1>
        <p>Transform your long, clunky URLs into short, shareable links within seconds.</p>

        <form action="/short" method="POST">
            <label for="longUrl">Enter Long URL:</label>
            <input type="text" id="longUrl" name="longUrl" placeholder="https://example.com" required>
            <button type="submit">Shorten</button>
        </form>

        <% if(shortUrl) { %>
        <div class="short-url">
            <p>Your Shortened URL: <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a></p>
        </div>
        <% } %>
    </div>
</body>
</html>
